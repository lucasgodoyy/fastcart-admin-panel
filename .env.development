# ═══════════════════════════════════════════════════════════
#  fastcart-admin-panel — Local Development
# ═══════════════════════════════════════════════════════════

# Backend API (running locally outside Docker)
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# Storefront URL for preview (store running locally)
# NEXT_PUBLIC_STOREFRONT_URL=http://localhost:3000
